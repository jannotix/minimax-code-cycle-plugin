{
  "schema": "cycle.mavis-model-strategy.v1",
  "strategy": "session-inherited",
  "recordEffectiveModel": true,
  "comment": "MiniMax Code 3.0.68 setup does not claim per-agent model YAML. Each role records the effective session model returned by the native runtime. A native-agent assignment is accepted only after a write/read round-trip proves that the current build honors it.",
  "roles": {
    "architect": "session-inherited",
    "executor": "session-inherited",
    "functional_reviewer": "session-inherited",
    "security_reviewer": "session-inherited",
    "arbiter": "session-inherited"
  }
}
